{
  "$description": "PureSharp Merge Skill configuration. DATA ONLY. All merge behaviour and all safety policy are defined in .claude/skills/merge-skill/SKILL.md, which is the SSOT. Only the keys below are configurable; everything else (explicit human approval, fail-closed gating, HEAD binding, auto-merge disabled) is fixed policy and is deliberately absent so it cannot be switched off. An unknown key, a wrong type, or requiredApprovals < 1 is a CONFIG ERROR and blocks the merge.",
  "requiredApprovals": 1,
  "mergeMethod": "ff-only",
  "deleteBranchAfterMerge": true
}
